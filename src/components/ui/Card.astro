---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`bg-card border border-border rounded-2xl p-6 md:p-8 hover:border-[#3F3F46] transition-colors duration-200 ${className}`}>
  <slot />
</div>
